import {ModuleSlug} from '@/modules/slugs'
import {createClientModule} from '@/modules/utils/createModule'

export const openCityModule = createClientModule({
  name: 'openCityModule',
  slug: ModuleSlug['open-city'],
})
